# Gemini API key for Workers server-side calls only (never expose to frontend)
GEMINI_API_KEY=

# Gemini model name used by worker/services/ai/gemini.ts
GEMINI_MODEL=gemini-2.0-flash

# Admin bearer token required by all /api/* endpoints (GET/POST)
ADMIN_TOKEN=
