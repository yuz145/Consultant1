{
  "name": "relationship-advisor-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=esm --platform=browser --target=es2020 --outfile=public/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
